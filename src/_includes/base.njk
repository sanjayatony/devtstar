<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
</head>

<body class="font-blex p-4 max-w-screen-md mx-auto prose prose-slate prose-sm italic">
  {% if layout == "post.njk" %}
    <div class="mb-6">
      <a href="/" class="text-3xl">..</a>
    </div>
  {% endif %}
  <header>
    <h1 class="text-3xl font-bold italic">{{ title }}</h1>
  </header>
  <main class="">
    <article>
      {{ content | safe }}
    </article>
  </main>
</body>

</html>
